[tool.poetry]
name = "kifurushi"
version = "0.1.0"
description = "Simple library to forge network data"
authors = ["lewoudar <lewoudar@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
attrs = "^20.3.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
scapy = "^2.4.4"
bandit = "^1.7.0"
flake8 = "^3.8.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
